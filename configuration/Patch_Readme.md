Patch for downgrade at valid Kernel and Mesa drivers


Fix sound and other problems
```
git clone https://aur.archlinux.org/downgrade.git
cd downgrade
makepkg -si
sudo downgrade linux (elegir versión 6.18.1.arch1)
```
